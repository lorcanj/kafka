AssignmentState has mapProcessToClientStateRebalanceDTO to be used during the rebalance

assignActiveStatefulTasks() assigns active tasks to the HAAClientState

balanceTasksOverThreads() optimises this (ok for the active version)

populateNewActiveAssignments() assigns these to the newAssignments

optimiseActiveStatefulTasks() does rack aware balancing on the newAssignments map

--> now mapProcessToClientStateRebalanceDTO is stale

1. Update mapProcessToClientStateRebalanceDTO with up to date assignment

want to populate ClientState map with mapProcessToClientStateRebalanceDTO

2. populate ClientState map

Will there be a problem if there is missing data as HAAClientState does not contain all the information?




TODO: double check how ClientState should be updated and what needs to be updated

create another branch where create interface for ClientState and HAAClientState


